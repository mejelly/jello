<% if !@user.nil? %>
<div class="col-lg-12" id="commentArea">

<%#= form_tag('/addcomment', method: "get", class: "") do %>
    <%= hidden_field_tag 'current_gist_id', current_gist_id %>
    <div class="form-group">
      <%= text_area_tag 'comment', nil, class: "", placeholder: "Leave a comment"  %>
    </div>
    <div class="form-group"><button class="btn btn-default submitcomment"><span class="glyphicon glyphicon-comment"></span> Comment</button>
    </div>
<% #end %>
<% end %>

<div id="loadhere">
  <%= render 'comment', comments: @comments %>
</div>

</div>