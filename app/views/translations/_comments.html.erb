<% if !@user.nil? %>

<div class="col-lg-12" id="commentArea">
  <div class="commentTitle">Comments:
    <%= if(!comments.nil?)
          JSON.parse(comments).count
        else 0
        end
    %>
  </div>
<%#= form_tag('/addcomment', method: "get", class: "") do %>
    <%= hidden_field_tag 'current_gist_id_', current_gist_id %>
    <div class="commentAvatar pull-left">
      <img class="" src="<%= @user[:info][:image] %>" />
    </div>
    <div class="form-group">
      <div class="arrow-wrapper">
        <div class="arrow-inner"></div>
        <div class="arrow-outer"></div>
      </div>
      <%= text_area_tag 'comment', nil, class: "", placeholder: "Leave a comment"  %>
    </div>
    <div class="form-group"><button class="btn btn-default submitcomment pull-right"><span class="glyphicon glyphicon-comment"></span> Comment</button>
      <br clear="all">
    </div>
<% #end %>
<% end %>

<div id="loadhere">
  <%= render 'comment', comments: @comments %>
</div>

</div>