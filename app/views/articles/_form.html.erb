<div class="col-lg-12" id="newArticle">
  <%= form_for(article) do |f| %>
    <div class="form-group row">
        <% if article.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

              <ul>
                <% article.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
              </ul>
            </div>
        <% end %>
    </div>

    <div class="form-group row">
        <!--<label for="articleTitle" class="col-sm-2 col-form-label">user_id</label>-->
        <div class="col-sm-10">
          <%= f.hidden_field :user_id, :value => @user[:extra][:raw_info][:user_id],  class: 'form-control' %>
        </div>
    </div>

    <div class="form-group row">
      <label for="articleTitle" class="col-sm-2 col-form-label">Title</label>
      <div class="col-sm-10">
        <%= f.text_field :title, class: 'form-control', required: true %>
      </div>
    </div>
    <div class="form-group row">
      <label for="articleUrl" class="col-sm-2 col-form-label">URL</label>
      <div class="col-sm-10">
        <%= f.text_field :url, class: 'form-control' %>
      </div>
    </div>
    <div class="form-group row">
      <label for="articleContent" class="col-sm-2 col-form-label">Content</label>
      <div class="col-sm-10">
        <%= f.text_area :content, class: 'form-control enableMediumEditor', id:'article_content', rows: 10 %>
      </div>
    </div>
    <div class="form-group row">
      <div class="pull-right col-sm-10">
        <%= f.submit class: 'btn btn-primary' %>
      </div>
    </div>
  <% end %>
</div>